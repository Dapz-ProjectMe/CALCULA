const sidebar=document.getElementById("sidebar"),menuBtn=document.getElementById("menuBtn"),themeBtn=document.getElementById("themeBtn");menuBtn.addEventListener("click",()=>sidebar.classList.toggle("open"));function showToast(t){const x=document.getElementById("toast");x.textContent=t;x.classList.add("show");setTimeout(()=>x.classList.remove("show"),1800)}function applyTheme(){const dark=CalcStorage.getTheme()==="dark";document.body.classList.toggle("dark",dark);themeBtn.textContent=dark?"☀":"☾"}themeBtn.addEventListener("click",()=>{const next=CalcStorage.getTheme()==="dark"?"light":"dark";CalcStorage.saveTheme(next);applyTheme()});function route(){const id=location.hash.replace("#","")||"dashboard";document.querySelectorAll(".page").forEach(p=>p.hidden=true);const target=document.getElementById(id)||document.getElementById("dashboard");target.hidden=false;document.querySelectorAll(".nav-item").forEach(n=>n.classList.toggle("active",n.dataset.page===target.id));document.getElementById("pageTitle").textContent=target.id==="calculator"?"Calculator":"Dashboard";sidebar.classList.remove("open")}window.addEventListener("hashchange",route);applyTheme();route();let deferredPrompt;window.addEventListener("beforeinstallprompt",e=>{e.preventDefault();deferredPrompt=e;const btn=document.getElementById("installBtn");btn.hidden=false;btn.onclick=async()=>{deferredPrompt.prompt();await deferredPrompt.userChoice;deferredPrompt=null;btn.hidden=true}});